# PimpBunny Plugin for Hedon Haven

This plugin follows the Hedon Haven external JavaScript plugin contract.

## Build

Run from either the repository root or `src`:

```bash
./src/compile.sh
```

The generated `plugin.zip` must contain `plugin.yaml` and `bundle.js` at its root when installed in Hedon Haven.
